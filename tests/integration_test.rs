//! Hello world example for Rust.
//! This is a crate description, needed or else missing_docs warning will occur.

use lib_template_rust;

#[test]
fn it_add_one() {
    assert_eq!(2, lib_template_rust::add_one(1));
}
